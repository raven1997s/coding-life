server:
  port: 48942
spring:
  cloud:
    nacos:
      config:
        server-addr: 1.116.121.207:8848
        file-extension: yml # 使用的 Nacos 配置集的 dataId 的文件拓展名，同时也是 Nacos 配置集的配置格式，默认为 properties
        namespace: 55d3b2ea-0d86-4c48-ae60-05c9f7dbff7f # 使用的 Nacos 的命名空间，默认为 null
        name: com.raven.nacos # 使用的 Nacos 配置集的 dataId，默认为 spring.application.name
        group: raven-nacos # 使用的 Nacos 配置分组，默认为 DEFAULT_GROUP
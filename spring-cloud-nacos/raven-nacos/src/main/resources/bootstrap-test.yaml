spring:
  cloud:
    nacos:
      config:
        server-addr: 1.116.121.207:8848
        file-extension: yml # 使用的 Nacos 配置集的 dataId 的文件拓展名，同时也是 Nacos 配置集的配置格式，默认为 properties
        namespace: 8798f07a-b777-47fb-bd2b-9fa811ccccdb # 使用的 Nacos 的命名空间，默认为 null
        name: nacos-config # 使用的 Nacos 配置集的 dataId，默认为 spring.application.name
        group: TEST_GROUP # 使用的 Nacos 配置分组，默认为 DEFAULT_GROUP